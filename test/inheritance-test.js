import expect from 'expect';

describe('inheritance', function () {

  /*  it.only('should behave', function () {
        class A {
            state = {a: 1};
        }
        class B extends A {
            state = {b: 2};
        }
        class C extends B {
            state = {c: 3};
        }
        var c = new C();

        expect(state.c).toBe(3);
    });*/


});